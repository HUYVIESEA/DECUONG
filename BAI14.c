#include"stdio.h"
int main(void) {
    printf("Nhap phan tu mang\n");
    int i, n;
    scanf("%d", &n);
    int a[n];
    //
    for (i = 0; i < n; i++)
        scanf("%d", &a[i]);
    for (i = 0; i < n; i++)
        printf("%d ", a[i]);
    //
    printf("\n");
    int le[n],chan[n];
    int j=1,k=1;
    for (i = 0; i < n; i++)
        if (a[i] % 2 == 0)
        {

        }
    else
        {

        }
    for(i=0;i<n;i++)
        printf("%d",chan[i]);
        printf("%d",le[i]);

}